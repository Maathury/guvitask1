# Login & Registration (Spring Boot)

Requirements:
- Java 17
- Maven 3.9+
- MySQL 8+

Configure database in `src/main/resources/application.properties`, then run:

```
mvn spring-boot:run
```

Notes:
- This minimal example does not include Spring Security. Passwords are hashed with SHA-256 for demo purposes only. Do not use as-is in production.